export interface RadioButtonProps {
  /** Whether the radio button is selected */
  isSelected: boolean;
  /** Size of the radio button (default: 20) */
  size?: number;
  /** Color of the radio button when selected */
  color?: string;
  /** Border color of the radio button (defaults to color) */
  borderColor?: string;
}
